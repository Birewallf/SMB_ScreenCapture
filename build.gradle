group 'scap'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.7

repositories {
    mavenCentral()
}

dependencies {
}

jar {
    manifest {
        attributes 'Main-Class': 'bwg.App'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}
